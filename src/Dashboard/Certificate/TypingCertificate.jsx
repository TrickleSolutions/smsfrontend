import { Button } from '@material-tailwind/react'
import React from 'react'

const TypingCertificate = ({ back }) => {
    return (
        <div>TypingCertificate
            <Button onClick={back}>Back to Certificate</Button>
        </div>
    )
}

export default TypingCertificate